spring:
  application:
    name: kakeibo-api          
  datasource:                  
    url: jdbc:mysql://localhost:3306/kakeibo
    username: app
    password: password
  jpa:
    hibernate:
      ddl-auto: none           
    open-in-view: false
management:
  endpoints:
    web:
      exposure:
        include: health        # Actuatorの公開エンドポイントをhealthのみに絞る